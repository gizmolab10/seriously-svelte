export enum ButtonIDs {
  help = '?',
}
export enum DBTypes {
  firebase = 'f',
  airtable = 'a',
}
export enum BulkIDs {
  public = 'Public',
  mine   = 'Jonathan Sand',
}
export enum PersistenceIDs {
  details = 'details',
  db      = 'db',
}

